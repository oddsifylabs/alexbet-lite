{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install && echo $(git rev-parse --short HEAD) $(git log -1 --format=%ai) $(git log -1 --format=%s) $(git log -1 --format=%an) > .commit-info.txt || echo 'unknown' > .commit-info.txt"
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "always",
    "restartPolicyMaxRetries": 5,
    "timestamp": "2026-04-23T23:45:00Z"
  }
}
